{
	"name": "obsidian-svelte-ui-root",
	"private": true,
	"scripts": {
		"build": "pnpm --filter ./obsidian-svelte-ui run build",
		"lint": "prettier --check .",
		"format": "prettier --write .",
		"changeset": "changeset",
		"release": "pnpm run build && pnpm changeset publish"
	},
	"devDependencies": {
		"@changesets/cli": "^2.31.0",
		"prettier": "^3.8.1",
		"prettier-plugin-svelte": "^3.5.1"
	}
}
